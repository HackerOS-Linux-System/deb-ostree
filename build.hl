// cmake
// make

> mkdir build
> cd build
> cmake ..
> make

;; > apt install libsolv-dev
;; > apt install libostree-dev
;; > apt install pkg-config
;; > apt install -y build-essential
;; install mising dependeces ^^^^^^^
